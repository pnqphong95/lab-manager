/*!
 * Lab manager custom rule
 * Author: pnqphong
 * 
 */
(function(factory) {
	if (typeof define === "function" && define.amd) {
		define([ "jquery", "./jquery.validate" ], factory);
	} else if (typeof module === "object" && module.exports) {
		module.exports = factory(require("jquery"));
	} else {
		factory(jQuery);
	}
}(function($) {

	$.validator.addMethod("dateLessThanEqualToday", function(value, element) {
		var givenDate = new Date(value);
		givenDate.setHours(0, 0, 0, 0);
		var today = new Date();
		today.setHours(0, 0, 0, 0);
		return this.optional(element) || givenDate <= today;
	}, "Vui nhập không nhập ngày trong tương lai.");

	return $;
}));